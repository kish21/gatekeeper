[project]
name = "gatekeeper"
version = "0.0.1"
description = "GateKeeperAI — a tool-agnostic, verifiable governance gateway for MCP. Every tool call authenticated, RBAC-checked, approved-on-write, and recorded in a tamper-evident hash-chained ledger."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "Kishore" }]
keywords = ["mcp", "model-context-protocol", "governance", "gateway", "rbac", "audit", "ai-security"]

# --- Runtime (prod) dependencies. Pins added in /foundation when first installed. ---
dependencies = [
  "mcp>=1.27.2,<2",      # official MCP SDK; 2.x renamed the client API (streamable_http_client, httpx2) — migrate deliberately
  "pydantic>=2.13.5",         # typed contracts / DTOs
  "pydantic-settings>=2.15.0",# .env-driven settings (no hardcoding)
  "python-dotenv>=1.2.2",    # read .env values for upstream credential refs ({from_env: NAME})
  "pyyaml>=6.0.3",           # YAML config loader
  "typer>=0.26.7",         # operator CLI
  "rich>=15.0.0",            # CLI output
  "fastapi>=0.141.1",      # HTTP MCP transport + (M2) approval API
  "uvicorn[standard]>=0.52.4",
  "httpx>=0.28.1",         # async HTTP for upstream/LLM
  "sqlalchemy>=2.0.52",       # ledger persistence
  "alembic>=1.19.1",       # DB migrations (never hand-edit schema)
  "cedarpy",             # Cedar policy engine (policy-as-code, analyzable)
  "PyJWT[crypto]>=2.13.0",   # OIDC identity adapter (M3.2): JWKS signature/aud/exp validation
]

[project.optional-dependencies]
# M2 AI layer — install only when building risk-scoring. Keeps M1 lean.
ai = ["anthropic>=0.111.0"]
# Demo governed targets — real, third-party MCP servers used to prove "govern ANY server, zero code"
# (M1.4). NOT a gateway runtime dependency: it is a server GateKeeper governs, registered purely in
# config/upstreams.yaml. Install with `pip install -e ".[demo]"`.
demo = ["mcp-server-time>=2026.6.4"]
# Hosted, durable ledger — a Postgres database instead of the local SQLite file, so the audit trail
# survives a restart and more than one replica can serve. Install with `pip install ".[postgres]"`;
# the container image ships with it.
postgres = ["psycopg[binary]>=3.2.12"]

[dependency-groups]
# Dev/test tooling — NOT shipped to prod.
dev = [
  "pytest>=9.1.1",
  "pytest-asyncio>=1.4.0",
  "pytest-cov>=7.1.0",
  "pytest-timeout>=2.4.0",   # a hung async test must FAIL with stacks, never wedge CI

  "ruff>=0.16.5",
  "mypy>=2.3.1",
  "types-PyYAML",
  "pip-audit>=2.10.1",
  "psycopg[binary]>=3.2.12",   # the durable-ledger integration tests run against a real Postgres
  "pre-commit>=4.6.2",
]

[project.scripts]
gatekeeper = "gatekeeper.cli.app:app"   # `gatekeeper serve|tail|verify|show`

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gatekeeper"]

# --- src-layout so the installed package == the tested package -------------
[tool.ruff]
src = ["src", "tests"]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "S"]  # incl. flake8-bandit (S) for security lint
ignore = ["S101"]                                   # allow assert in tests

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S"]

# Typer declares a CLI by calling Option()/Argument() in the signature — that IS the framework's
# interface, not the mutable-default bug B008 looks for.
[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["typer.Option", "typer.Argument"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["gatekeeper"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
# Fail-loud on a wedged test (dumps all stacks). "thread" method: signal-based does not work on
# Windows; generous budget — the slowest legit test (real subprocess upstream) is well under it.
timeout = 120
timeout_method = "thread"
