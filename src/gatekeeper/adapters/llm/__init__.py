"""LLM adapters (implement ports.LLMProvider), selected via config adapters.llm. M2 only.

claude.py — Claude Haiku risk classifier (default).
stub.py   — deterministic, offline provider for tests (no network, no cost).
(openai.py — optional alternative; same interface.)
"""
