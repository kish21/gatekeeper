"""Pure domain logic and value objects (Decision, Principal, Verdict, RiskScore).

No I/O, no vendor SDKs — fully unit-testable. The "what" the gateway means, independent of how
it talks to MCP, Cedar, SQLite, or an LLM.
"""
