"""Typed DTOs that cross boundaries (pydantic): ToolCall, ToolResult, LedgerEntry, Decision.

These are the typed contracts between transport, gateway, adapters, and the ledger. Defined in
/contracts. No raw dicts across a boundary.
"""
